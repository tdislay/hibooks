import { Controller } from "@nestjs/common";

@Controller()
export class UsersController {
  // constructor(private usersService: UsersService) {}
  // In an enterprise context, a change email / password route should be added
}
